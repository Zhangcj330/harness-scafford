# Project Memory

- Use `AGENTS.md` and `docs/` as the source of truth.
- Runs are durable under `.runs/`.
- Workspaces are isolated under `.worktrees/`.
- Codex Chat should preview tasks before writing tracked task files.
- Repo memory is stored in `memory/`; task memory is stored under `tasks/`.
